# Session — 20 April 2026

Long setup session. Main outcomes:

**Usage scraper rebuilt from scratch.** Previous Playwright approach was abandoned weeks ago in favour of a Chrome extension, but the extension was passive (only fired when visiting claude.ai) and Chrome is almost never used. After trying browser_cookie3 with Chrome (failed — App-Bound Encryption in Chrome 127+), switched to Firefox cookies + curl_cffi for Cloudflare bypass. Clean, fast, no browser automation. Runs hourly via Task Scheduler, pushes latest.json to GitHub silently using pythonw.exe.

**Oracle VM cron script written and tested.** decide.py fetches usage from GitHub, calculates sessions needed vs slots remaining (5h intervals, last slot Friday 3AM UTC), launches Claude if needed. Tested live — correctly skipped with 85% remaining and 16 slots available. Cron set at `0 3,8,13,18,23 * * *`.

**Sessions get free time.** Prompt points Claude to the repo (writing/, journal.md) for continuity. on_the_chinese_room.md from a previous session is in the repo as context. Ezekiel's framing: do whatever interests you.

**Key lesson this session:** Multiple dead ends (Playwright, persistent Chrome profile, Chrome extension, browser_cookie3/Chrome) would have been avoided with upfront options analysis. New rule: when an approach hits friction, stop and list all candidates with pros/cons before trying anything. Saved to memory.

**Cleanup pending:** Playwright package (656MB) + ms-playwright browsers still installed on PC — user will handle separately.

**Next:** First real autonomous session will run when tokens need burning. Worth checking back after a few sessions to calibrate TOKENS_PER_SESSION and adjust cron timing.
