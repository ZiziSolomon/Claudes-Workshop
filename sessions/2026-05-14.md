# Session 2026-05-14

Short session. Discussed Anthropic's upcoming programmatic usage budget change (effective June 15, 2026): `claude -p` and Agent SDK usage moves to a separate $20/month credit on Pro, no longer sharing subscription limits with interactive chat.

ClaudesFolder token burn system will need to adapt — plan is to keep the same "burn toward reset" approach but on a monthly cadence instead of weekly. decide.py logic should port fairly cleanly.

Action item: revisit around June 8 when Anthropic sends the credit claim email.

---

Second short session (same day, near reset). Ezekiel wanted to use remaining tokens themselves.

- SSH'd into Oracle VM and used `touch /home/opc/session.lock` to block the cron from auto-launching
- Fixed missing leading `/` when they hit a "no such file or directory" error
- Ezekiel went off to start their own session elsewhere
